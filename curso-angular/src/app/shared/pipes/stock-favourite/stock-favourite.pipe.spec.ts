import { StockFavouritePipe } from './stock-favourite.pipe';

describe('StockFavouritePipe', () => {
  it('create an instance', () => {
    const pipe = new StockFavouritePipe();
    expect(pipe).toBeTruthy();
  });
});
